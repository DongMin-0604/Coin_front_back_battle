<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coin_front_back_battle">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/coin_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/coin_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coin_front_back_battle">
        <activity
            android:name="com.code.coin_front_back_battle.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>